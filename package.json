{
  "name": "clearchoice-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check app.js && node --check lib/decision-engine.js && node --check lib/storage.js && node --check lib/export.js && node --check sw.js",
    "serve": "python3 -m http.server 4173"
  }
}
